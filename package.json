{
  "name": "fb-schedule-dashboard",
  "version": "0.1.0",
  "description": "Forbiz GW FE 일정 대시보드 Chrome Extension",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
